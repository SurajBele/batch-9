apiVersion: v1
kind: ReplicationController
metadata:
  name: myReplicationController
spec:
selector:
  app: myworkapp
  template:
  metadata:
  name: spider2
  namespace: mywork
  labels:
    env: dev
    app: myworkapp
spec:
  containers:
    - name: spider2
      image: nginx
      ports:
        - containerPort: 80
...
